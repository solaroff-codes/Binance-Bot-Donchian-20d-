# strategy/

Combines `indicators/` outputs into entry/exit signals. Should be
config-driven — instrument, timeframe, and parameter set passed in, not
hardcoded — so the same strategy module runs against GC, CL, ES, and NQ
without duplication. Not yet built.
